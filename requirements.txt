langgraph>=1.2
langchain>=1.3
langchain-core>=1.4
sqlalchemy>=2.0
pymssql>=2.3
python-dotenv>=1.0

# LLM providers — install the one(s) you need
langchain-ollama>=0.2
langchain-anthropic>=0.3
langchain-openai>=0.3


# Vanna Text-to-SQL (v2+ is a different product — pin to 0.7.x)
vanna==0.7.9
chromadb>=0.5

# API + UI  (fastapi>=0.116 required: starlette>=0.40 compatibility with streamlit)
fastapi>=0.116
uvicorn>=0.30
httpx>=0.27
streamlit>=1.35